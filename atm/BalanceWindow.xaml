<Window x:Class="WpfApp.BalanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Balance Check" Height="200" Width="400">
    <Grid Background="#FF492567">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Current Balance -->
        <TextBlock Grid.Row="0" Text="Current Balance:" FontSize="16" Margin="10"/>

        <!-- Balance Text -->
        <TextBlock x:Name="BalanceTextBlock" Grid.Row="1" FontSize="16" Margin="10" HorizontalAlignment="Center">
            <TextBlock.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </TextBlock.RenderTransform>
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <!-- Back Button -->
        <Button Grid.Row="2" Content="Back to Main Page" Click="BackToMain_Click" FontSize="16" Margin="0,53,0,-33" HorizontalAlignment="Center"/>
    </Grid>
</Window>
